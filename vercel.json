{
  "buildCommand": "cd frontend/gymrat-marketplace && npm install && CI=false npm run build",
  "outputDirectory": "frontend/gymrat-marketplace/build",
  "installCommand": "echo skip",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
